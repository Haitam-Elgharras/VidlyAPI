# all the the file that we need to ignore in our vidly api generated by github copilot here :
# to ignore node modules
node_modules
# to ignore the .env file
.env
# to ignore the .env file
.env.test
# to ignore the .json file
*.json
# to ignore the .lock file
*.lock
# to ignore the .log file
*.log

Notes.txt
